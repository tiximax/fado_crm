{
  "name": "fado-crm-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright E2E tests for FADO CRM frontend",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install:browsers": "playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.2",
    "jsonwebtoken": "^9.0.2"
  }
}
